{
  "Bus": {
    "ConnectionString": "amqp://localhost:5672",
    "ProductInfoQueue": "ProductInfo"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DatabasePostGres": "Server=${POSTGRES_HOST};Username=${POSTGRES_USER};Database=${POSTGRES_DB};SSLMode=Prefer;Port=${POSTGRES_PORT};Password=${POSTGRES_PASSWORD}"
  },
  "HubMinifiedBaseUrl": "http://localhost:8081/api/"
}